// fork: prism (fork-owned file)
import { createFileRoute } from "@tanstack/react-router";
import { PrismPage } from "../fork/prism/PrismPage";
export const Route = createFileRoute("/prism")({ component: PrismPage });
